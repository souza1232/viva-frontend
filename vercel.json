{
  "buildCommand": "chmod +x node_modules/.bin/expo && npx expo export --platform web && mv dist/index.html dist/_app.html && cp -r public/. dist/",
  "outputDirectory": "dist",
  "framework": null,
  "routes": [
    { "src": "^/$", "dest": "/index.html" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/_app.html" }
  ]
}
