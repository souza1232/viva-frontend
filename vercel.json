{
  "buildCommand": "chmod +x node_modules/.bin/expo && npx expo export --platform web && cp -r public/. dist/",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/inicio", "destination": "/inicio.html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
